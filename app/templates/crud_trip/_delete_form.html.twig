<form method="post" action="{{ path('delete', {'id': trip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trip.id) }}">
    <br>
    <button class="btn btn-primary">Delete</button>
</form>
